stages:
- name: Codeception test
  steps:
  - runScriptConfig:
      image: php:7.2
      shellScript: |-
        apt-get update
        apt-get install -y --no-install-recommends git zip libsqlite3-dev zlib1g-dev
        docker-php-ext-install zip
        curl --silent --show-error https://getcomposer.org/installer | php
        ./composer.phar install -n --prefer-dist
        touch storage/testing.sqlite storage/database.sqlite
        cp .env.testing .env
        php artisan migrate
        php artisan migrate --env=testing --database=sqlite_testing --force
        ./vendor/bin/codecept build
        ./vendor/bin/codecept run
- name: Publish image
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: php-example:${CICD_EXECUTION_SEQUENCE}
- name: Deploy
  steps:
  - applyYamlConfig:
      path: ./deploy/deployment.yaml
notification:
  recipients:
  - recipient: https://oapi.dingtalk.com/robot/send?access_token=55294e2c78c147bb1a845c1fae261d505ab9849f94b6fd62a1109273e6ded38a
    notifier: c-lf6t6:n-vtsp6
  condition:
  - Success
  - Changed
  - Failed
